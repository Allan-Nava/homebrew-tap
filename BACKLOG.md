# Backlog — homebrew-tap

Sorgente unica dei todo di questo repo. Id stabili `HT-n`; spuntare, non cancellare.

> **Sync automatico issue**: questo file è la fonte di verità. Ogni `HT-n` diventa una issue GitHub (label `backlog`, milestone per sezione) via `scripts/backlog-sync.sh` + workflow `.github/workflows/backlog-sync.yml`. Spuntare un item (`[x]`) chiude la issue al prossimo push, togliere la spunta la riapre. Idempotente. **Non aprire/chiudere le issue a mano: si edita qui.**
>
> Due automazioni scrivono issue oltre a questo sync: il watcher dei fix upstream (apre `Fix upstream arrivato: HT-n` quando un fix atterra nel cask generato) e il report della CI (apre `Tap rotto: la CI dei cask è rossa` e la richiude appena torna verde).

Attenzione al confine: qui si distribuisce, non si sviluppa. Diversi item **si chiudono con un commit upstream** — nel `.goreleaser.yaml` (`homebrew_casks`) di [`checkfleet`](https://github.com/Allan-Nava/checkfleet), [`segcheck`](https://github.com/Allan-Nava/segcheck) o [`ladder-bench`](https://github.com/Allan-Nava/ladder-bench) — non toccando `Casks/`: quei file sono generati e vengono riscritti a ogni release. Ogni item dice dove va fatto il lavoro.

## Automazione del tap

- [x] **HT-3 — CI del tap**: `.github/workflows/cask-ci.yml` — `brew style` + caricamento di ogni cask, shellcheck degli script, install reale su `macos-14`/`macos-13` (versione del binario == versione del cask, quarantine rimossa, smoke `check tcp`), install del bundle desktop, `brew audit --cask --online`. Su push/PR, ogni lunedì e a mano. Due vincoli scoperti provando: i cask vanno validati **dentro un tap** (Homebrew rifiuta `--cask ./Casks/x.rb`) → `scripts/ci-place-tap.sh`; e `brew style` va dato sui **file** `Casks/*.rb`, non sul tap, perché su un tap gira anche shfmt sugli script e `--fix` tronca l'heredoc del renderer. _(qui)_
- [x] **HT-4 — Cask `checkfleet-desktop`**: l'app Wails era pubblicata nelle release ma non installabile da brew. `scripts/render-desktop-cask.sh` risolve l'asset `checkfleet-desktop_v*_darwin_universal.zip`, ne calcola lo sha256, verifica che `checkfleet.app` sia alla radice dello zip e rende il cask (`app`, `depends_on :macos`, postflight anti-quarantine, `zap` su Caches/Preferences/WebKit/SavedState di `com.wails.checkfleet`); `.github/workflows/desktop-cask.yml` lo aggiorna (dispatch + cron 6h + manuale), valida con `brew style`/`brew info` e committa solo se cambia, con rebase in caso di gara col push di GoReleaser. _(qui; verificato con style/info/`audit --online` su v0.125.0)_
- [ ] **HT-5 — Trigger immediato del cask desktop** (**upstream**): step finale in `desktop.yml` che fa `repository_dispatch` di tipo `checkfleet-desktop-release` su questo repo con `client_payload.tag`. Serve un PAT con `contents:write` sul tap (riusare `HOMEBREW_TAP_GITHUB_TOKEN`). Senza questo il cask desktop si aggiorna col cron, quindi con qualche ora di ritardo.
- [x] **HT-10 — Notifica su tap rotto**: job `report` in `cask-ci.yml` — raccoglie l'esito di style/load, shellcheck, install CLI, install desktop e audit online e tiene **una sola** issue di stato (`Tap rotto: la CI dei cask è rossa`) via `scripts/gh-issue.sh`: la apre col dettaglio di cosa è fallito e il link al run, la richiude da sola appena torna verde. Zero commenti a ogni run (nessun rumore), skip sulle PR dove il token è read-only. _(qui)_
- [x] **HT-17 — Backlog → issue**: `scripts/backlog-sync.sh` (parser awk degli item `HT-n`, label `backlog`, milestone = sezione, create/close/reopen idempotenti, `--dry-run`) + `.github/workflows/backlog-sync.yml` su push che toccano `BACKLOG.md`/lo script/il workflow e a mano. Stesso schema di `cmd/backlog-sync` upstream ma in shell: qui non c'è toolchain Go e non vale portarcela per un tap. _(qui; provato in dry-run: 16 item, 4 milestone)_
- [x] **HT-18 — Watcher dei fix upstream**: `scripts/check-upstream-fixes.sh` audita `Casks/checkfleet.rb` e dice quali fix attesi da upstream sono atterrati (HT-1 completions, HT-2 `desc` senza articolo, HT-7 `caveats`); il job `upstream-fixes` in `cask-ci.yml` apre una issue `Fix upstream arrivato: HT-n` quando uno atterra e la richiude se torna pendente. Gira sui push, cioè anche sul commit del bot che porta il cask nuovo: è esattamente il momento in cui il fix diventa visibile. _(qui; provato in dry-run su cask reale e su cask finto coi fix)_
- [x] **HT-20 — Guard sui riferimenti tra job**: `scripts/check-workflows.rb` nel job `shellcheck` — verifica che ogni `needs` punti a un job esistente e che ogni `needs.<job>.result` sia dichiarato. Nasce da un caso reale: rimosso il job `shellcheck` senza togliere il riferimento in `report`, GitHub ha registrato un run `failure` **senza job e senza log** (workflow invalido, non è un fallimento di test). Provato sul commit rotto: due `::error::` ed exit 1. _(qui)_
- [x] **HT-22 — `brew style` diviso: cask generati vs cask nostri**: con l'arrivo di segcheck e ladder-bench il job `lint` è andato rosso (4 offense su `macos-latest`, 19 con Homebrew 6.0.18 in locale) su file che **non si possono correggere qui** — `Cask/StanzaOrder`/`Cask/StanzaGrouping` sui blocchi `on_macos`/`on_linux`, `Layout/FirstArrayElementIndentation` sul `depends_on formula: [` di ladder-bench, `Layout/EmptyLinesAroundBlockBody`: tutte del **template di GoReleaser**, non del nostro contenuto (upstream ladder-bench l'aveva già verificato: spostare le dipendenze in un `custom_block` le fa emettere prima di `version` e si passa da 3 offense a 13). Le due vie alternative sono chiuse: `.rubocop.yml` del tap ignorato (HT-14) e `brew style --fix` vietato (tronca l'heredoc del renderer). Quindi: due bucket in `cask-ci.yml`, divisi dal marker `DO NOT EDIT` in testa al file e non da un elenco di nomi — i cask generati da GoReleaser passano con `--except-cops`, i cask che rende `render-desktop-cask.sh` passano **senza nessuna esclusione** (verificato: `checkfleet-desktop.rb` è pulito a zero esclusioni, e `desktop-cask.yml` ora lo stila strict). Così un cask nuovo che arriva col commit del bot non fa cadere il lint e non porta con sé uno sconto sul nostro template. Le esclusioni dovute a **noi** hanno un item che le riporta a zero: HT-2 (`Cask/Desc`) e HT-21 (`Style/NumericPredicate`); quelle del template GoReleaser restano finché non le sistema goreleaser. _(qui)_
- [x] **HT-24 — Token GitHub per l'audit**: il job `audit` moriva con `API rate limit exceeded for <ip> ... Rate limit exceeded for core resource (60 limit)` → `exception while auditing checkfleet-desktop`. Causa: `brew audit --online` interroga l'API di GitHub e senza token il limite è 60 richieste/ora **per IP**, che su un runner hosted è condiviso con altri utenti — passare da due a quattro cask ci ha portato oltre. Non è un fallimento del tap ma lo sembra: Homebrew conta l'eccezione come errore di audit (`cask/audit.rb` → `add_error`, poi `ofail`), quindi job rosso e issue "Tap rotto" aperta a sproposito. Fix: `HOMEBREW_GITHUB_API_TOKEN: ${{ github.token }}` nell'`env` di `cask-ci.yml` e `desktop-cask.yml` — 1000 richieste/ora, token del run già disponibile e read-only (`permissions: contents: read`), nessun secret da creare né PAT che scade. _(qui; verificato che sia la variabile giusta per questo code path: con un token finto lo stesso audit esce con `GitHub API Error: Bad credentials`)_
- [x] **HT-25 — La leg intel della matrice non parte più**: in `install-cli` la matrice è `[macos-14, macos-13]`, ma `macos-13` **non è più un runner disponibile** (non è nella lista dei label di GitHub — `actionlint` lo segnala come sconosciuto) e il job resta appeso in `queued`: sul run 32135457989 tutti gli altri job erano finiti e quella leg era ancora in coda. Due danni: la copertura intel del cask generato non viene più verificata da nessuno, e `report` (che ha `install-cli` in `needs`) non parte finché GitHub non decide di scaricare la leg, quindi la issue di stato arriva in ritardo o su un esito `cancelled`. Risolto spostando la matrice su `macos-15-intel`, che è un runner Intel vero (non Rosetta su arm) ed è nella lista dei label validi — `actionlint` non segnala più niente. _(qui)_
- [x] **HT-26 — Detector di drift col sync automatico**: il tap si aggiorna da sé (GoReleaser pusha a ogni tag `v*`) ma quel push ha un modo di fallire che non accende nessuna spia — i PAT fine-grained **scadono per forza**, e quando scadono la release upstream pubblica archivi e immagine e poi muore sull'ultimo passo: dal quel momento il tap serve una versione vecchia e **tutto resta verde** (il cask si carica, l'audit passa), perché `livecheck { skip }` è voluto (HT-15) e nessuno confronta con upstream. Fatto: `scripts/sync-cask-versions.sh` confronta la `version` di ogni cask generato con `repos/<progetto>/releases/latest` (l'endpoint `latest` esclude draft **e prerelease**, quindi una rc non bumpa niente) e in `--apply` riscrive `version` e i `sha256` **scaricando gli asset veri** della release — mai un bump a mano. Tre guard-rail: tocca solo i file col marker `DO NOT EDIT` di GoReleaser, rifiuta se `sha256` e `url` non sono adiacenti (il pairing è per posizione), e scarta la riscrittura se il diff toccherebbe righe che non sono `version`/`sha256`. `.github/workflows/cask-sync.yml` lo gira ogni 6h (cron sfasato da `desktop-cask.yml`, stesso gruppo `tap-write`) su ubuntu — la validazione vera la fa Cask CI sul push che ne esce — committa e tiene una issue di stato, che resta aperta anche dopo il sync perché **il sync cura il sintomo, non la causa**: se il token upstream è scaduto, il prossimo tag si ferma di nuovo. _(qui; verificato falsificando `Casks/abrsim.rb` a 0.4.0 con sha finti: `--apply` ha ricostruito il file **identico byte per byte** all'output di GoReleaser, e i due guard-rail provati su copie sporche rifiutano lasciando il file intatto)_
- [x] **HT-27 — README generato dai cask**: il tap acquisisce progetti da sé, il README no — `abrsim` è entrata con due commit del bot ed è rimasta fuori da README, CLAUDE.md e AGENTS.md finché non se n'è accorto qualcuno. Un elenco scritto a mano di una cosa che cresce da sola invecchia per costruzione. Fatto: `scripts/render-readme-casks.sh` rigenera **tre regioni fra marker** (comandi di install, tabella "What's in here", comandi di uninstall) leggendo solo `Casks/*.rb` — niente rete, niente brew: `desc`, `homepage`, `name`, artifact (`binary` → CLI, `app` → app macOS-only), piattaforme dai blocchi `on_macos`/`on_linux` × `on_intel`/`on_arm`, e `depends_on formula:` → "(needs `ffmpeg`)". La prosa attorno resta a mano: è la parte utile e non si deduce da un cask. `--check` esce 1 col diff. `cask-sync.yml` lo gira a ogni push su `Casks/**` (oltre al cron) e committa; `cask-ci.yml` si limita a un `::warning::`, perché un rosso per un ritardo di documentazione aprirebbe "Tap rotto" a sproposito (la lezione di HT-19) mentre su main il sync ripara da solo in un minuto. **Limite noto**: la licenza non è derivabile — il DSL dei cask non ha `license` (HT-13) — quindi quel paragrafo resta a mano; se servisse, andrebbe letta da `gh api repos/<o>/<r> --jq .license.spdx_id`, ma vorrebbe dire dare rete e auth a uno script che oggi non ne ha bisogno. _(qui; verificato: seconda passata idempotente, e un cask finto `newtool` copiato in `Casks/` compare da solo in tutte e tre le regioni con `--check` che esce 1)_
- [ ] **HT-23 — Install reale per segcheck, ladder-bench e abrsim**: il job `install-cli` installa e fa smoke **solo** su checkfleet; per gli altri tre cask la CI si ferma a style, load e `audit --online` (che scarica gli asset, quindi lo sha256 è comunque coperto), ma nessuno verifica che il binario installato risponda e che la sua versione sia quella del cask — cioè il controllo che ha più valore. Da generalizzare in matrice per cask, con il comando di versione e lo smoke di ognuno (`segcheck version`? `ladder-bench doctor`? `abrsim version`?) e la stessa assert su quarantine. Attenzione al costo: il cask di ladder-bench dipende da **ffmpeg**, quindi quella leg si porta dietro l'install della formula (minuti, non secondi) — valutare se tenerla fuori dalla matrice per-push e lasciarla al cron del lunedì.
- [x] **HT-19 — `ci.yml` (template `brew test-bot`) rimosso**: era **rosso per costruzione**, confermato dal run reale [30242314209](https://github.com/Allan-Nava/homebrew-tap/actions/runs/30242314209) — `brew test-bot --only-tap-syntax` stila l'intero tap: 7 file, **8 offense**, di cui `Cask/Desc` e `Layout/EmptyLinesAroundBlockBody` sul cask generato da GoReleaser (riscritto a ogni release, non correggibili qui e non escludibili da test-bot, che ignora il `.rubocop.yml` del tap), più `shfmt.sh: Fixable bad styles` su **tutti e cinque** gli script e SC2250/SC2292/SC2310 con la house-style di Homebrew. Nessun valore unico perso: `--only-tap-syntax` = style + readall, e `cask-ci.yml` fa style mirato sui `Casks/*.rb` con esclusioni motivate, load di ogni cask, shellcheck default, install su due arch con assert sulla versione e audit online; il `readall --aliases --os=all --arch=all` è stato portato nel job `lint` — è l'unica cosa che test-bot faceva in più, perché `brew info --cask` da macOS non valuta il ramo `on_linux` del cask generato. Una CI perennemente rossa insegna solo a ignorare la CI. _(qui)_

## Contenuto dei cask (lavoro upstream)

HT-1, HT-2, HT-7 e HT-21 sono sorvegliati da `scripts/check-upstream-fixes.sh` (job `upstream-fixes`): quando il fix atterra nel cask generato arriva una issue `Fix upstream arrivato: HT-n` con cosa fare qui. Non serve ricontrollarli a mano.

- [ ] **HT-1 — Completions shell nel cask** (**upstream**): la CLI ha `checkfleet completion bash|zsh|fish` ma il cask installa solo il binario, quindi da brew nessuno le ha. In `.goreleaser.yaml` → `homebrew_casks`:
  ```yaml
  generate_completions_from_executable:
    executable: checkfleet
    args: ["completion"]
    shells: [bash, zsh, fish]
  ```
  Reso verificato con goreleaser 2.17 (`generate_completions_from_executable "checkfleet", "completion", shells: [:bash, :zsh, :fish]`): Homebrew risolve il nome sul binario **staged** e lo esegue a install-time passando il nome shell come argomento nudo — combacia con la CLI. Se falliscono sono warning, non install rotta. Da aggiungere poi un assert in `cask-ci.yml` (i file completion esistono dopo l'install).
- [ ] **HT-2 — `desc` senza articolo** (**upstream**): `brew style` segnala `Cask/Desc: Description shouldn't start with an article` su `desc "A fleet of..."`. Si sistema cambiando `description:` in `.goreleaser.yaml` (es. `Fleet of domain-aware infrastructure checks in one binary`). Quando la release successiva porta qui il cask corretto, togliere `Cask/Desc` da `--except-cops` in `cask-ci.yml` (resta solo `Layout/EmptyLinesAroundBlockBody`, che è il template di GoReleaser e non è correggibile da nessuna parte).
- [ ] **HT-7 — `caveats` nel cask CLI** (**upstream**): campo `caveats:` in `homebrew_casks` con due righe utili post-install (config d'esempio `checkfleet.example.yml`, link a `checkfleet check --help` e alle docs). Verificato che goreleaser lo rende come blocco `caveats <<~EOS`.
- [ ] **HT-8 — Versione reale nel bundle desktop** (**upstream**): `checkfleet.app/Contents/Info.plist` dichiara `CFBundleVersion`/`CFBundleShortVersionString` `1.0.0` fissi. Cosmetico per Homebrew (`brew upgrade` confronta la versione del cask, non il plist) ma bugiardo in "Info su" e nel Finder: iniettare la versione nel build Wails.
- [x] **HT-21 — `exit_status == 0` nel postflight di segcheck** (**upstream**): il postflight anti-quarantine di `Casks/segcheck.rb` fa `if system_command("/usr/bin/xattr", args: ["-h"]).exit_status == 0`, che `brew style` segnala come `Style/NumericPredicate` (è **l'offense che ha reso rossa la CI**, insieme a HT-22). Fix nel `.goreleaser.yaml` di segcheck: `.exit_status.zero?`, o meglio la forma che usa già checkfleet (`if system_command "/usr/bin/xattr", args: ["-h"], print_stderr: false`, senza confronto sull'exit status). **Atterrato**: nessun cask generato usa più quella forma (segcheck e abrsim scrivono `.exit_status.zero?`), quindi `Style/NumericPredicate` è stato tolto da `--except-cops` e il cop torna a coprire i cask generati — verificato: 4 file, zero offense. Il watcher lo riporta ora come `done`. _(qui, per la parte tap)_

- [ ] **HT-9 — Desktop su Linux**: le release hanno anche `checkfleet-desktop_v*_linux_amd64.tar.gz`. Un cask con solo `binary` è installabile su Linuxbrew 6+, ma `app` no (artifact macOS-only), quindi servirebbe un cask separato o un ramo `on_linux` con `binary`. Valutare se qualcuno lo usa davvero prima di aggiungere superficie.

## Documentazione

- [x] **HT-6 — README del tap**: cosa contiene il tap, comandi espliciti con `--cask` (install/upgrade/uninstall/zap), nota sul *trust* dei tap di terze parti in Homebrew 6+, nota su Linux (i cask con solo `binary` si installano su Linuxbrew, quelli con `app` no), puntatore a "il contenuto è generato". _(qui)_
- [x] **HT-11 — Regole per gli agent**: `AGENTS.md` + `CLAUDE.md` con il confine generato/a-mano, il divieto di editare `Casks/`, i gate (`brew style`/`audit`) e le trappole (postflight quarantine, `livecheck skip`, race col push di GoReleaser). _(qui)_

## Deciso: non fare

- [x] **HT-12 — Formula per Linux**: ~~aggiungere `brews:` in goreleaser per coprire Linuxbrew~~. **Non serve**: un cask i cui artifact sono solo `binary` non è macOS-only — `Cask::Artifact::Binary` non è in `MACOS_ONLY_ARTIFACTS` — quindi `checkfleet` si installa già su Linuxbrew 6+, i blocchi `on_linux` del cask generato sono reali e il postflight `xattr` si auto-salta lì (è dietro un `system_command "/usr/bin/xattr", args: ["-h"]`). Una formula in più sarebbe solo un secondo posto dove sbagliare la versione. _(deciso 2026-07-26)_
- [x] **HT-13 — Stanza `license` nei cask**: ~~esporre PolyForm-Noncommercial nel cask~~. **Impossibile**: il DSL dei cask non ha `license` (non è in `Cask::DSL::DSL_METHODS`); il campo `license:` di goreleaser viene accettato e **scartato silenziosamente** in fase di render (verificato). La licenza resta nel repo upstream e negli archivi. _(deciso 2026-07-26)_
- [x] **HT-14 — `.rubocop.yml` nel tap**: ~~escludere i cop del file generato da config invece che da CLI~~. **Non funziona**: `brew style` forza `--config $(brew --prefix)/Library/.rubocop.yml`, quindi la config del tap viene ignorata (verificato: le esclusioni non hanno effetto né nel repo né dentro Library/Taps). Le esclusioni vivono in `cask-ci.yml` come `--except-cops`, con puntatore a HT-2. _(deciso 2026-07-26)_
- [x] **HT-15 — `livecheck` reale**: ~~sostituire `skip` con un livecheck sui tag upstream~~. **Non fare**: la versione la spinge la release (goreleaser per il CLI, `desktop-cask.yml` per l'app). Un livecheck sarebbe una seconda fonte di verità che può divergere. _(deciso 2026-07-26)_
- [x] **HT-16 — `zap` per il cask CLI**: ~~aggiungere path da ripulire~~. **Non serve**: la CLI non scrive stato in `~/Library` né in home — la history è opt-in con `--history PATH` esplicito. Il `# No zap stanza required` generato è corretto. (Il cask desktop invece ha `zap`: l'app usa WebKit e le preferenze del bundle.) _(deciso 2026-07-26)_
